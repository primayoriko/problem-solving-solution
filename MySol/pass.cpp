#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    int c=0;
    while(s[c]!='\0'){
        c++;
    }
    int dp[c];
    memset(dp,-1,sizeof(dp));
    if(c<=2){
        cout<<"just a legend"<<endl;
        return 0;
    }
    for(int i=1;i<n-1;i++){
        if()
    }
}
