#include <bits/stdc++.h>
using namespace std;
map <int, vector<int>>;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int a,b,c,d,e,n,m,i,j;
    cin>>n>>m;
    int p[m+1];
    vector <int> q[m+1];
    memset(p,-1, size(p));
    for(i=0;i<m;i++){
        cin>>a;
        for(j=0;j<a;j++){
            cin>>a;
            q[j].push_back(a);
        }
    }




}
