#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n,a,b,i,j,k,m;
    cin>>n;
    pair <int, int> q[n];
    for(i=0;i<n;i++){
        cin>>a>>b;
        q[i].first=a;
        q[i].second=b;
    }

}
